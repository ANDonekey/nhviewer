# Execution Rules

- Always use PowerShell script execution
- Never run complex commands inline
- Use:
  powershell.exe -File .codex_tmp/script.ps1

- Store scripts in:
  .codex_tmp/

- Avoid CMD syntax